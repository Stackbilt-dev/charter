ADF: 0.1

🧠 STATE:
  CURRENT: v1.9.1 release prepared for npm. The patch restores legacy ADF trigger compatibility, fixes per-trigger match observability and tidy pointer restoration, freezes an exact routing-harness snapshot, and refreshes the public evidence register with reproducible, unfavorable, and cross-repo findings.
  NEXT: Publish/tag v1.9.1 through npm trusted publishing. Use the exact-routing failures as the baseline for routing-quality improvements. #200 (authority-gated governance, P1 — propose→gate→commit contract). #90 (package ecosystem — configurable dependency orchestration). #201 (tiered execution contract, P2).
  BLOCKED_ON: Nothing critical. #116 (charter_brief via gateway) deferred to commercial layer. #200 is design-first before implementation.
  COMPLETED: Phase 1-7, M0-M2, blast + surface subsystems, compile pipeline (outbound ADF→vendor), conformance suite (adf-spec/adf), MCP setup diagnostics (#191), ADF tidy routing (#198), displayAiDir compiler fix (#207), docs-sync graceful flags (#195), README cleanup (#206), Rust/WASM first-class support (#230), scaffold-core output-quality fixes (#246 follow-through), and the August 2026 evidence refresh (CSA-003).
