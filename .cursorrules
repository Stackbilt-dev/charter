# GENERATED by charter adf compile — DO NOT EDIT. Source of truth: .ai/ modules. Re-run: charter adf compile --target all --write

# .cursorrules

> Charter Kit development context router

## TASK

Charter Kit development rules

## CONTEXT

- PNPM monorepo with project references
- Package flow: types <- git, classify, validate, drift, blast, surface, ci <- cli
- blast and surface are zero-dependency analysis packages (no @stackbilt/types)
- ADF package is zero-dependency and self-contained
- Repo uses SemVer for published packages and machine-facing governance contracts
- Conventional Commits: feat(scope):, fix(scope):, docs:, chore:
- .charter/ holds governance config: config.json, patterns/*.json, policies/*.md
- CLI command handlers in packages/cli/src/commands/, config in src/config.ts

## CONSTRAINTS _(load-bearing)_

- TypeScript strict mode, ES2022 target, CommonJS output
- 2-space indent, semicolons, single quotes, kebab-case filenames
- Pure functions in library packages; side effects only in CLI command handlers
- Import across packages via @stackbilt/* aliases (tsconfig.base.json)
- All commands must support --format json for stable machine-readable output
- Exit codes: 0 success, 1 policy violation, 2 runtime/usage error
- Treat JSON schemas, exit-code meanings, and generated artifact conventions as semver-governed contracts
- Breaking contract changes require explicit semver classification and migration notes in roadmap/release docs
- Commits via scripts/smart-commit.sh only (never raw git add/commit)
- Run scripts/smart-commit.sh --dry-run first to preview grouped commits
- Validation gates: pnpm run typecheck, pnpm run build, pnpm run test
- Use pnpm exec tsc instead of npx tsc in WSL
- Bias toward action over clarification; implement rather than ask when task is clear

## METRICS _(load-bearing)_

- `adf_commands_loc`: 905 / 1041 [lines]
- `adf_bundle_loc`: 175 / 202 [lines]
- `adf_sync_loc`: 217 / 250 [lines]
- `adf_evidence_loc`: 272 / 313 [lines]
- `adf_migrate_loc`: 688 / 792 [lines]
- `bundler_loc`: 125 / 144 [lines]
- `parser_loc`: 214 / 247 [lines]
- `cli_entry_loc`: 258 / 297 [lines]

## BUDGET_RATIONALES

adf_commands_loc_2026-05-22: 0 -> 905, ceiling 800 -> 1041; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
adf_bundle_loc_2026-05-22: 0 -> 175, ceiling 200 -> 202; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
adf_sync_loc_2026-05-22: 0 -> 217, ceiling 250 -> 250; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
adf_evidence_loc_2026-05-22: 0 -> 272, ceiling 380 -> 313; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
adf_migrate_loc_2026-05-22: 0 -> 688, ceiling 750 -> 792; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
bundler_loc_2026-05-22: 0 -> 125, ceiling 500 -> 144; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
parser_loc_2026-05-22: 0 -> 214, ceiling 300 -> 247; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts
cli_entry_loc_2026-05-22: 0 -> 258, ceiling 250 -> 297; updateEvidence MCP tool and adf patch changes[] added in v0.16.0 — both features intentionally expand commands/ and serve.ts

## STATE

- **CURRENT**: v0.10.0 published to npm (2026-04-09) — all 11 `@stackbilt/*` packages live. Publish identity: stackbilt-kovermier. Added blast + surface subsystems (deterministic codebase analysis, zero-LLM). First cross-linked consumer is cc-taskrunner 1.5.0 via CC_BLAST_* env var gate.
- **NEXT**: #94 (blast: exclude .claude/worktrees/ ghost paths — Claude Code consumers see inflated blast radius from ephemeral worktree clones). #90 (package ecosystem — configurable dependency orchestration via charter config). #69 (typed data access disambiguation policy).
- **BLOCKED_ON**: Nothing critical. #94 is a quick win, #90 + #69 are larger design-first efforts.
- **COMPLETED**: Phase 1-7, M0-M2, #50-53 (bootstrap UX, migrate parser, auto-loading, CLAUDE.md coexistence), blast + surface subsystems (new in 0.10.0), first npm publish of @stackbilt/blast + @stackbilt/surface, vendor fix for cli scaffold-response types (#93 — dropped file: dep on unpublished @stackbilt/contracts to unblock publish).

# agents-modules
## Additional instructions

Read the matching file before working in these areas:

- `.ai/adf-dev.adf` — Triggers: ADF, parser, formatter, patcher, bundler, metric, weight
- `.ai/governance.adf` — Triggers: validate, drift, audit, trailer, risk, governance
- `.ai/classifier.adf` — Triggers: classifier, routing, auto-route, adf add, classify, rule placement
- `.ai/analysis.adf` — Triggers: blast, surface, dependency graph, blast radius, route extraction, schema extraction
- `.ai/typed-data-access.adf` — Triggers: tenant, user, subscription, quota, credit, mrr, pii, sensitivity, data registry, ontology, disambiguation, DATA_AUTHORITY, raw D1, service boundary, auth_scoped, billing_critical
# /agents-modules
