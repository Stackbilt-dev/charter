/**
 * materializer/project — project file generator stubs
 *
 * Generates project-level files (package.json, tsconfig, README, etc.) for the scaffold output.
 * @stub Implementation pending — see child issue for materializer module.
 */

import type { ScaffoldFacts, ScaffoldFile } from '../types';

/**
 * Generate project-level ScaffoldFiles for the given scaffold facts.
 *
 * @stub Throws until the materializer module implementation lands.
 */
export function generateProjectFiles(_facts: ScaffoldFacts): ScaffoldFile[] {
  throw new Error(
    'Not implemented: use @stackbilt/scaffold-core@x.y when materializer/ module lands'
  );
}
