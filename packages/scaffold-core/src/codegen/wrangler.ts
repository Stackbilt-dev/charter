/**
 * codegen/wrangler — generateWranglerBindings stub
 *
 * Generates wrangler.toml binding declarations from ScaffoldBinding list.
 * @stub Implementation pending — see child issue for codegen module.
 */

import type { ScaffoldBinding } from '../types';

/**
 * Generate wrangler.toml binding TOML fragment for the given bindings.
 *
 * @stub Throws until the codegen module implementation lands.
 */
export function generateWranglerBindings(_bindings: ScaffoldBinding[]): string {
  throw new Error(
    'Not implemented: use @stackbilt/scaffold-core@x.y when codegen/ module lands'
  );
}
