/**
 * classify/patterns — PatternDef registry
 *
 * Stub: PATTERNS array is intentionally empty. Populated when the classify
 * module implementation lands in a child issue.
 */

import type { PatternDef } from '../types';

export type { PatternDef };

/**
 * The canonical list of supported scaffold patterns.
 * @stub Implementation pending — see child issue for classify module.
 */
export const PATTERNS: PatternDef[] = [];
