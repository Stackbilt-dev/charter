/**
 * classify/classifier — choosePattern stub
 *
 * Selects the best-matching PatternDef for a given intention string.
 * @stub Implementation pending — see child issue for classify module.
 */

import type { ClassifyResult } from '../types';

/**
 * Choose the best pattern for the given intention string.
 *
 * @stub Throws until the classify module implementation lands.
 */
export function choosePattern(_intention: string): ClassifyResult {
  throw new Error(
    'Not implemented: use @stackbilt/scaffold-core@x.y when classify/ module lands'
  );
}
