/**
 * classify/bindings — inferBindings stub
 *
 * Infers Cloudflare Worker binding declarations from pattern traits.
 * @stub Implementation pending — see child issue for classify module.
 */

import type { ScaffoldBinding } from '../types';

/**
 * Infer required Cloudflare Worker bindings from pattern name and traits.
 *
 * @stub Throws until the classify module implementation lands.
 */
export function inferBindings(_pattern: string, _traits: string[]): ScaffoldBinding[] {
  throw new Error(
    'Not implemented: use @stackbilt/scaffold-core@x.y when classify/ module lands'
  );
}
