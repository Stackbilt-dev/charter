/**
 * knowledge/decisions — ADR context/decision stubs
 *
 * Per-pattern ADR boilerplate strings used by the governance module.
 * @stub Implementation pending — see child issue for knowledge module.
 */

/**
 * Return the ADR context block for the given pattern.
 *
 * @stub Returns empty string until the knowledge module implementation lands.
 */
export function adrContextByPattern(_pattern: string): string {
  return '';
}

/**
 * Return the ADR decision block for the given pattern.
 *
 * @stub Returns empty string until the knowledge module implementation lands.
 */
export function adrDecisionByPattern(_pattern: string): string {
  return '';
}
